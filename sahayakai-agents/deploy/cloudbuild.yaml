# Cloud Build pipeline for sahayakai-agents.
# Invoked manually in Phase 1; hooked to a trigger later.
#
#   gcloud builds submit --config=deploy/cloudbuild.yaml \
#     --project=sahayakai-b4248 --region=asia-southeast1 .

substitutions:
  _REGION: asia-southeast1
  _REPO: sahayakai
  _SERVICE: sahayakai-agents

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: E2_HIGHCPU_8

steps:
  - id: test
    name: python:3.12-slim
    entrypoint: bash
    args:
      - -ceu
      - |
        pip install --quiet --upgrade pip
        pip install --quiet '.[dev]'
        pytest tests/unit -q

  - id: build
    name: gcr.io/cloud-builders/docker
    args:
      - build
      - --tag=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:$SHORT_SHA
      - --tag=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:latest
      - .
    waitFor: [test]

  - id: push
    name: gcr.io/cloud-builders/docker
    args:
      - push
      - --all-tags
      - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}

  # Staging deploy only. Production deploy is a manual follow-up so humans
  # review traces before traffic routes.
  - id: deploy-staging
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: gcloud
    args:
      - run
      - deploy
      - ${_SERVICE}-staging
      - --image=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:$SHORT_SHA
      - --region=${_REGION}
      - --project=${PROJECT_ID}
      - --no-traffic
      - --min-instances=1
      - --max-instances=3

images:
  - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:$SHORT_SHA
  - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:latest
