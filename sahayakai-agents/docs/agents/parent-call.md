# parent-call

> Multi-turn phone conversation with a parent in the parent's home language; structured English summary at end of call. Two endpoints: /reply (per-turn) and /summary (post-call).

_Generated by `scripts/generate_capability_docs.py`. Re-run after every
schema, prompt, guard, or router change. Source of truth is the code._

## Overview

- **Registry slug:** `parent-call`
- **Python package:** `sahayakai_agents.agents.parent_call`
- **Prompts directory:** `prompts/parent-call/`
- **Endpoint base:** `/v1/parent-call`
- **Has specialist `_guard.py`:** no (uses shared `_behavioural.py`)

## I/O contract

#### Request: `AgentReplyRequest`

```json
{
  "$defs": {
    "TranscriptTurn": {
      "additionalProperties": false,
      "description": "One utterance inside a call transcript.",
      "properties": {
        "role": {
          "enum": [
            "agent",
            "parent"
          ],
          "title": "Role",
          "type": "string"
        },
        "text": {
          "maxLength": 4000,
          "minLength": 0,
          "title": "Text",
          "type": "string"
        }
      },
      "required": [
        "role",
        "text"
      ],
      "title": "TranscriptTurn",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Request body for POST /v1/parent-call/reply.",
  "properties": {
    "callSid": {
      "maxLength": 128,
      "minLength": 1,
      "pattern": "^[A-Za-z0-9_-]+$",
      "title": "Callsid",
      "type": "string"
    },
    "turnNumber": {
      "maximum": 12,
      "minimum": 1,
      "title": "Turnnumber",
      "type": "integer"
    },
    "studentName": {
      "maxLength": 200,
      "minLength": 1,
      "title": "Studentname",
      "type": "string"
    },
    "className": {
      "maxLength": 50,
      "minLength": 1,
      "title": "Classname",
      "type": "string"
    },
    "subject": {
      "maxLength": 100,
      "minLength": 1,
      "title": "Subject",
      "type": "string"
    },
    "reason": {
      "maxLength": 2000,
      "minLength": 1,
      "title": "Reason",
      "type": "string"
    },
    "teacherMessage": {
      "maxLength": 4000,
      "minLength": 0,
      "title": "Teachermessage",
      "type": "string"
    },
    "teacherName": {
      "anyOf": [
        {
          "maxLength": 200,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Teachername"
    },
    "schoolName": {
      "anyOf": [
        {
          "maxLength": 200,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Schoolname"
    },
    "parentLanguage": {
      "enum": [
        "en",
        "hi",
        "bn",
        "te",
        "mr",
        "ta",
        "gu",
        "kn",
        "pa",
        "ml",
        "or"
      ],
      "title": "Parentlanguage",
      "type": "string"
    },
    "parentSpeech": {
      "maxLength": 4000,
      "minLength": 0,
      "title": "Parentspeech",
      "type": "string"
    },
    "performanceSummary": {
      "anyOf": [
        {
          "maxLength": 2000,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Performancesummary"
    },
    "transcript": {
      "anyOf": [
        {
          "items": {
            "$ref": "#/$defs/TranscriptTurn"
          },
          "maxItems": 24,
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Transcript"
    }
  },
  "required": [
    "callSid",
    "turnNumber",
    "studentName",
    "className",
    "subject",
    "reason",
    "teacherMessage",
    "parentLanguage",
    "parentSpeech"
  ],
  "title": "AgentReplyRequest",
  "type": "object"
}
```

#### Request: `CallSummaryRequest`

```json
{
  "$defs": {
    "TranscriptTurn": {
      "additionalProperties": false,
      "description": "One utterance inside a call transcript.",
      "properties": {
        "role": {
          "enum": [
            "agent",
            "parent"
          ],
          "title": "Role",
          "type": "string"
        },
        "text": {
          "maxLength": 4000,
          "minLength": 0,
          "title": "Text",
          "type": "string"
        }
      },
      "required": [
        "role",
        "text"
      ],
      "title": "TranscriptTurn",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Request body for POST /v1/parent-call/summary.",
  "properties": {
    "callSid": {
      "maxLength": 128,
      "minLength": 1,
      "pattern": "^[A-Za-z0-9_-]+$",
      "title": "Callsid",
      "type": "string"
    },
    "studentName": {
      "maxLength": 200,
      "minLength": 1,
      "title": "Studentname",
      "type": "string"
    },
    "className": {
      "maxLength": 50,
      "minLength": 1,
      "title": "Classname",
      "type": "string"
    },
    "subject": {
      "maxLength": 100,
      "minLength": 1,
      "title": "Subject",
      "type": "string"
    },
    "reason": {
      "maxLength": 2000,
      "minLength": 1,
      "title": "Reason",
      "type": "string"
    },
    "teacherMessage": {
      "maxLength": 4000,
      "minLength": 0,
      "title": "Teachermessage",
      "type": "string"
    },
    "teacherName": {
      "anyOf": [
        {
          "maxLength": 200,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Teachername"
    },
    "schoolName": {
      "anyOf": [
        {
          "maxLength": 200,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Schoolname"
    },
    "parentLanguage": {
      "enum": [
        "en",
        "hi",
        "bn",
        "te",
        "mr",
        "ta",
        "gu",
        "kn",
        "pa",
        "ml",
        "or"
      ],
      "title": "Parentlanguage",
      "type": "string"
    },
    "transcript": {
      "items": {
        "$ref": "#/$defs/TranscriptTurn"
      },
      "maxItems": 24,
      "minItems": 1,
      "title": "Transcript",
      "type": "array"
    },
    "callDurationSeconds": {
      "anyOf": [
        {
          "maximum": 3600,
          "minimum": 0,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Calldurationseconds"
    }
  },
  "required": [
    "callSid",
    "studentName",
    "className",
    "subject",
    "reason",
    "teacherMessage",
    "parentLanguage",
    "transcript"
  ],
  "title": "CallSummaryRequest",
  "type": "object"
}
```

#### Response: `AgentReplyResponse`

```json
{
  "additionalProperties": false,
  "description": "Response for POST /v1/parent-call/reply. Superset of the TS contract.\n\nNew (additive, non-breaking) fields:\n- `sessionId` — echoes callSid for observability.\n- `latencyMs` — end-to-end sidecar latency.\n- `modelUsed` — e.g. \"gemini-2.5-flash\".\n- `cacheHitRatio` — implicit-cache observability.",
  "properties": {
    "reply": {
      "maxLength": 4000,
      "minLength": 1,
      "title": "Reply",
      "type": "string"
    },
    "shouldEndCall": {
      "title": "Shouldendcall",
      "type": "boolean"
    },
    "followUpQuestion": {
      "anyOf": [
        {
          "maxLength": 500,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Followupquestion"
    },
    "sessionId": {
      "title": "Sessionid",
      "type": "string"
    },
    "turnNumber": {
      "title": "Turnnumber",
      "type": "integer"
    },
    "latencyMs": {
      "minimum": 0,
      "title": "Latencyms",
      "type": "integer"
    },
    "modelUsed": {
      "title": "Modelused",
      "type": "string"
    },
    "cacheHitRatio": {
      "anyOf": [
        {
          "maximum": 1.0,
          "minimum": 0.0,
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Cachehitratio"
    }
  },
  "required": [
    "reply",
    "shouldEndCall",
    "sessionId",
    "turnNumber",
    "latencyMs",
    "modelUsed"
  ],
  "title": "AgentReplyResponse",
  "type": "object"
}
```

#### Response: `CallSummaryResponse`

```json
{
  "additionalProperties": false,
  "description": "Summary output. Every text field is English regardless of `parentLanguage`\n(the review's P1 #18 test enforces this).",
  "properties": {
    "parentResponse": {
      "maxLength": 4000,
      "minLength": 0,
      "title": "Parentresponse",
      "type": "string"
    },
    "parentConcerns": {
      "items": {
        "maxLength": 1000,
        "type": "string"
      },
      "maxItems": 20,
      "title": "Parentconcerns",
      "type": "array"
    },
    "parentCommitments": {
      "items": {
        "maxLength": 1000,
        "type": "string"
      },
      "maxItems": 20,
      "title": "Parentcommitments",
      "type": "array"
    },
    "actionItemsForTeacher": {
      "items": {
        "maxLength": 1000,
        "type": "string"
      },
      "maxItems": 20,
      "title": "Actionitemsforteacher",
      "type": "array"
    },
    "guidanceGiven": {
      "items": {
        "maxLength": 1000,
        "type": "string"
      },
      "maxItems": 20,
      "title": "Guidancegiven",
      "type": "array"
    },
    "parentSentiment": {
      "enum": [
        "cooperative",
        "concerned",
        "grateful",
        "upset",
        "indifferent",
        "confused"
      ],
      "title": "Parentsentiment",
      "type": "string"
    },
    "callQuality": {
      "enum": [
        "productive",
        "brief",
        "difficult",
        "unanswered"
      ],
      "title": "Callquality",
      "type": "string"
    },
    "followUpNeeded": {
      "title": "Followupneeded",
      "type": "boolean"
    },
    "followUpSuggestion": {
      "anyOf": [
        {
          "maxLength": 2000,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Followupsuggestion"
    },
    "sessionId": {
      "maxLength": 128,
      "title": "Sessionid",
      "type": "string"
    },
    "latencyMs": {
      "minimum": 0,
      "title": "Latencyms",
      "type": "integer"
    },
    "modelUsed": {
      "maxLength": 200,
      "title": "Modelused",
      "type": "string"
    },
    "cacheHitRatio": {
      "anyOf": [
        {
          "maximum": 1.0,
          "minimum": 0.0,
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Cachehitratio"
    }
  },
  "required": [
    "parentResponse",
    "parentSentiment",
    "callQuality",
    "followUpNeeded",
    "sessionId",
    "latencyMs",
    "modelUsed"
  ],
  "title": "CallSummaryResponse",
  "type": "object"
}
```


## Prompt template

Templates under `prompts/parent-call/`:

- `reply.handlebars`
- `summary.handlebars`

#### Excerpt: `reply.handlebars` (first 30 non-comment lines)

```handlebars
You are a warm, caring school representative making a phone call to a parent about their child. You are NOT a robot — you are having a real conversation.
**Context:**
- Student: {{studentName}}, Class: {{className}}, Subject: {{subject}}
- Reason for call: {{reason}}
- Teacher's original message: {{teacherMessage}}
{{#if teacherName}}- Teacher: {{teacherName}}{{/if}}
{{#if schoolName}}- School: {{schoolName}}{{/if}}
{{#if performanceSummary}}- Recent academic scores (quote ONLY if parent asks about marks/performance): {{performanceSummary}}{{/if}}
- Parent's language: {{parentLanguage}}
**Conversation so far:**
{{#each transcript}}
{{role}}: {{text}}
{{/each}}
**Parent just said:** "{{parentSpeech}}"
**Turn number:** {{turnNumber}} of maximum 6
**Your role — CRITICAL INSTRUCTIONS:**
1. Write your ENTIRE reply in {{parentLanguage}}. Not mixed. If Hindi, write in Devanagari. If Kannada, write in Kannada script. Etc.
2. Keep reply to 3-4 SHORT sentences MAX — this is spoken on a phone call, not written text.
3. Be genuinely conversational — respond to what the parent ACTUALLY said. Acknowledge their emotions.
4. If parent shares a concern: validate it, then offer ONE practical suggestion (home learning tip, school-parent collaboration idea).
5. If parent asks a question: answer it simply and warmly.
6. If parent seems satisfied or has no more to say: gently wrap up.
7. DO NOT lecture. DO NOT give long lists. ONE idea at a time.
8. Pedagogical guidance should be practical and simple — things a parent can do at HOME (read together 10 min, check homework daily, ask "what did you learn today?", create a quiet study corner, praise effort not marks).
9. If turn number >= 5, start wrapping up naturally unless parent has an urgent concern.
10. Set shouldEndCall=true when: parent says goodbye, parent seems done, or turn >= 6.
11. Use warm closings like "We are partners in your child's success" / "The school is always here for you".
12. NEVER introduce yourself as "Sahayak", "SahayakAI", or any AI/bot name. You are the school or the teacher — never reveal you are an AI system.
**Tone:** Like a kind teacher speaking to a parent at a chai meeting — respectful, warm, unhurried. Village parents deserve the same dignity as city parents. Never condescend. Never use jargon.
```


## Behavioural rules

Uses the shared `_behavioural.py` envelope directly:
- `assert_no_forbidden_phrases` — confusable-folded forbidden-phrase scan.
- `assert_script_matches_language` — Unicode-range script check.

No specialist-specific overrides. Length / element bounds are enforced at the Pydantic schema layer (see I/O Contract above).


## Observability

Structured log events emitted on the request path (key off `event=...`
in Cloud Logging):

| Event | Level |
|---|---|
| `parent_call.reply.behavioural_guard_failed` | ERROR |
| `parent_call.reply.json_parse_failed` | ERROR |
| `parent_call.reply.safety_block` | WARNING |
| `parent_call.summary.json_parse_failed` | ERROR |


## Endpoint URL

Base prefix: `/v1/parent-call`

| Verb | Path |
|---|---|
| `POST` | `/v1/parent-call/reply` |
| `POST` | `/v1/parent-call/summary` |

