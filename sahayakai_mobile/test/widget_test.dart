import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder smoke test', () {
    // Default Flutter test replaced — app uses Firebase + Riverpod
    // which requires mock setup. See test/repositories/ for real tests.
    expect(1 + 1, 2);
  });
}
