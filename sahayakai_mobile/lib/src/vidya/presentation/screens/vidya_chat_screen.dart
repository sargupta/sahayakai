import 'package:flutter/material.dart';

class VidyaChatScreen extends StatelessWidget {
  const VidyaChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Vidya Chat')),
    );
  }
}
